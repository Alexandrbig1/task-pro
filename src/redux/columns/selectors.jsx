export const selectColumns = (state) => state.columns.columns.items;

export const selectIsLoading = (state) => state.columns.columns.isLoading;

export const selectError = (state) => state.columns.error;
